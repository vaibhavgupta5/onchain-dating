{
  "name": "hela-match-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for HelaMatch",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:hela": "hardhat run scripts/deploy.ts --network helaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10"
  }
}
